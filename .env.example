# StateStrike runtime configuration
# Honeypot target API
HONEYPOT_HOST=0.0.0.0
HONEYPOT_PORT=8000
HONEYPOT_URL=http://localhost:8000

# OpenEnv-compatible environment server
STATESTRIKE_ENV_HOST=0.0.0.0
STATESTRIKE_ENV_PORT=8001
STATESTRIKE_ENV_WS_URL=ws://localhost:8001/ws

# Dashboard configuration
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=8501

# Agent runtime configuration
TELEMETRY_FILE=telemetry.json
MAX_RETRIES=3
RETRY_BACKOFF_BASE_SECONDS=0.25
